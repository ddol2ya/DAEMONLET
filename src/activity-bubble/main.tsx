import { createRoot } from "react-dom/client"
import ActivityBubbleApp from "./ActivityBubbleApp"
import "./activity-bubble.css"
createRoot(document.getElementById("root")!).render(<ActivityBubbleApp />)
